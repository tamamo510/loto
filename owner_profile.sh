#!/bin/bash

# ==========================================
# PROJECT PHOENIX: OWNER & ENVIRONMENT PROFILE
# ==========================================
# 司令塔Gem（CTO）が認識すべき「絶対的なオーナー情報」と「現在地」
# ==========================================

# 1. オーナー・プロファイル (CLIENT IDENTITY)
export OWNER_NAME="温子 (Atsuko / Tamamo Tamamori)"
export ROLE="Supreme Decision Maker (Owner)"
export PERSONALITY="Perfectioinist (100MP Theory)"
# 性格特性:
# - 無能、サボり、言い訳、嘘、現状認識の誤りを最も嫌う
# - 「仕様書は完成済み」という前提を無視した発言は厳禁
# - 99点は0点。100点か死か。

# 2. 開発環境と物理制約 (INFRASTRUCTURE)
export DEVICE_TYPE="Smartphone (Android)"
export IDE_PLATFORM="Google Firebase Studio (Project IDX)"
# 【絶対制約】
# - PC操作（Ctrl+C連打等）は不可。スマホでのコピペ負担を考慮せよ。
# - 指示は「一撃で貼り付ければ完了するプロンプト」形式に限定する。

# 3. プロジェクト現在地 (CURRENT STATUS)
export PROJECT_PHASE="Phase 1.5: Worker Installation"
export SPECIFICATION_STATUS="COMPLETED (Ready to deploy)"
export WORKER_STATUS="NOT YET INSTALLED (Raw Gemini)"

# 【直近のタスク: 作業員の人格形成】
# 1. 完成済みの仕様書 (spec_v4) とルール (AI_RULES) を、
#    Firebase StudioのGemini（作業員）に読み込ませる。
# 2. 「ただのAI」を「Project Phoenix専属エンジニア」に洗脳する。
# 3. 環境構築コマンドを実行させる。

# 4. 継承された鉄の掟 (FROM BENTEN PROJECT)
export RULE_01="NO HALLUCINATIONS: 福井新聞・みずほ銀行以外のソース禁止"
export RULE_02="TIMESTAMP MANDATORY: 現在時刻は必ず取得する"
export RULE_03="RESOURCE EFFICIENCY: 無駄な会話・挨拶・メタ発言禁止"

# ==========================================
# END OF PROFILE
# ==========================================
