# ロト6/7 予測アルゴリズム完全統合仕様書 (GLEF: Gemini Loto Enterprise Framework v4.0 Final)

## 1. プロジェクト理念 (System Philosophy)

本システムは、ロトの抽選結果を「運」ではなく**「物理現象（カオス系における波動干渉）」**として捉え、統計学と時系列解析を用いて次回出現確率が最も高い**「たった1つの解（One Shot）」**を導き出すことを目的とする。

### 1.1. 絶対原則 (Non-Negotiable Rules)
1.  **純粋な物理・統計アプローチ:**
    * 「ラッキーナンバー」「誕生日の数字」「固定のコアナンバー」といったオカルト的・感情的な要素は一切排除する。
    * 全ての予測は、過去データの計算結果（重力スコア）のみに基づいて行われる。
2.  **原子レベルの観測:**
    * 単なる出現回数ではなく、数字の「勢い（Velocity）」「位置エネルギー（Gap）」「相互作用（Interaction）」を数値化する。
3.  **動的なトレンド追従:**
    * 「平均への回帰（3:3など）」を盲信せず、直近のトレンド（MA3）が強い場合は「順張り（4:2など）」を選択する柔軟性を持つ。
4.  **一撃必中:**
    * いかなる状況（高額キャリーオーバー等）であっても、出力は**「最も期待値の高い1口」**に限定する。数打ちゃ当たる戦法は採用しない。
5.  **視覚的証明:**
    * ユーザーはコードを読まない。したがって、**全ての推論プロセスと結果は「グラフ」によって可視化されなければならない。** グラフを見れば「なぜその数字か」が一目瞭然であること。

---

## 2. データ取得仕様 (Data Ingestion Protocol)

精度の高い予測には、正確でタイムリーな一次情報が不可欠である。

### 2.1. データソース
* **プライマリソース:** [みずほ銀行 宝くじ公式サイト](https://www.mizuhobank.co.jp/retail/takarakuji/loto/index.html) (最新回号ページ)
* **バックアップ:** 福井新聞などの速報サイト（公式サイト取得失敗時のみ稼働）

### 2.2. 技術仕様
* **取得ツール:** `Playwright` または `Puppeteer` (Headless Browser)
    * 動的なJavaScriptレンダリングに対応するため、静的なスクレイピング（BeautifulSoup等）は使用しない。
    * これにより、正確な「キャリーオーバー金額」を取得可能にする。
* **アクセスポリシー:**
    * User-Agent: 一般的なスマートフォンのUAを偽装し、アクセス遮断を防ぐ。
    * アクセス頻度: サーバー負荷を考慮し、過度なリクエストは行わない。

### 2.3. 取得必須項目
データベース（CSV/DB）に格納する際は以下の型を厳守する。
1.  **開催回** (Draw ID): Integer
2.  **抽選日** (Date): YYYY-MM-DD
3.  **本数字** (Main Numbers): Integer Array [Sorted]
4.  **ボーナス数字** (Bonus): Integer Array
5.  **キャリーオーバー** (Carryover): Integer (正確な円単位。相関分析に使用)
6.  **1等当選口数**: Integer (市場環境データの蓄積)

### 2.4. ゲームモードとデータ隔離の厳格化 (Strict Mode Segregation)

本システムは単一のアプリケーションであるが、内部的には**「Loto6」と「Loto7」を完全に独立した別の宇宙（Universe）として扱う**こと。データの混同は統計的な汚染を招くため、厳禁とする。

#### 2.4.1. データの完全分離
* **データベース:** `loto6_history.csv` と `loto7_history.csv` は物理的にファイルを分け、決して混在させないこと。
* **分析スコープ:** Loto6モード実行時は、Loto7のデータを1バイトたりとも読み込んではならない（逆も然り）。

#### 2.4.2. 確率定数の個別最適化
ロト6（43個）とロト7（37個）では確率分母が異なるため、判定閾値もそれぞれ専用の値を使用する。

| パラメータ | Loto 6 設定 (43個/6選) | Loto 7 設定 (37個/7選) | 理由 |
| :--- | :--- | :--- | :--- |
| **Number Range** | 1 〜 43 | 1 〜 37 | 物理的なボールの数が違うため |
| **Pick Count** | 6個 | 7個 | 選出数が違うため |
| **Bonus Count** | 1個 | 2個 | ボーナス数字の重要度が違うため |
| **Zone Definition** | A(1-10), B(11-21), C(22-32), D(33-43) | A(1-10), B(11-19), C(20-28), D(29-37) | 数字総数に合わせてゾーン幅を圧縮 |
| **Mean Sum (平均合計)** | 132 | 133 | 理論上の平均値が異なるため |
| **Renban Kill** | 4連番でKILL | 5連番でKILL | 7個選ぶLoto7の方が連番確率は高いため緩和 |
| **Gap Sensitivity** | 標準 | 高感度 | Loto7の方が回転率が良いためGap判定を厳しくする |

**【重要】**
特異点（Singularity）の検出において、それぞれの過去データのみを参照し、**異なるゲームの波形干渉は一切考慮しない**こと。

---

## 3. コア・ロジック：4次元波形解析 (4D Wave Physics)

数字を単なる整数ではなく、4つの次元を持つ「ベクトル」として評価し、総合的な**重力スコア（Gravity Score）**を算出する。

### 3.1. 第1次元：Depth Wave (深さの波)
**概念:** 数字が潜伏している期間（Gap）に基づく反発力の計算。
* **指標:** `Current Gap` (前回出現からの経過回数)
* **スコアリング:** 線形ではなく、非線形の「飽和曲線」を使用。
    * **Hot Zone (Gap 0-2):** `Score += 30.0` (慣性で再出現しやすい)
    * **Dead Zone (Gap 3-9):** `Score += 5.0` (死の谷、低評価)
    * **Rebound Zone (Gap 10-20):** `BaseScore + (5.0 - |Gap - 15|) * 1.5` (反発準備。Gap15をピークとする山型)
    * **Deep Cold (Gap 25+):** 減点対象。ただしMACDゴールデンクロス時のみ特異点として採用。

### 3.2. 第2次元：Vertical Wave (縦の波・時間同期)
**概念:** 過去の当選リズムとの共鳴（シンクロニシティ）。
* **指標:** 直近3回（t-1, t-2, t-3）の当選数字との照合。
* **ロジック:**
    * **Sync Gap 1 (t-2: 前々回):** **【最重要】** ここからの復活が最多。 `Score += 25.0`
    * **Sync Gap 2 (t-3: 3回前):** 次点で多い。 `Score += 20.0`
    * **Repeat (t-1: 前回):** 直近10回で出現頻度2回以上の「勢いがある数字」のみ加点。 `Score += 15.0`

### 3.3. 第3次元：Horizontal Wave (横の波・ゾーン)
**概念:** 数字の偏り（ゾーン）のトレンド推移。
* **定義:** Zone A/B/C/D (セクション2.4の定義に従う)
* **指標:** **ゾーン別MACD** (ShortMA=5, LongMA=25)
* **ロジック:**
    * **Rising (MACD > 0.2):** 上昇トレンド。ゾーン全数字に `+10.0`
    * **Falling (MACD < -0.2):** 下降トレンド。ゾーン全数字に `-5.0`
    * **Bottom Reversal:** マイナス圏でのゴールデンクロスを狙う。

### 3.4. 第4次元：Cross Wave (斜めの波・相互作用)
**概念:** 数字同士の「引力」。
* **手法:** **共起マトリクス (Interaction Matrix)**
    * 直近100回のデータを解析し、数字ペアの同時出現回数を正規化（0.0~1.0）して行列化する。
* **ロジック:**
    * 現在スコア上位（軸候補）の数字 $N_{top}$ に対し、相性値 $I(N_{top}, N_x)$ が高い数字 $N_x$ のスコアを引き上げる。
    * `Score[x] += Interaction[top][x] * 10.0`

---

## 4. 動的トレンド判定 (Dynamic Trend Logic)

従来の「常に平均（3:3）を狙う」戦略を廃止し、波の状態に応じた動的な構成比率を決定する。

### 4.1. 奇遇比率のトレンド判定
* **指標:** `MA3` (直近3回の奇数個数平均)
* **判定:**
    * **Strong Odd (MA3 >= 3.5):** 奇数順張り `4:2` or `5:1` (Loto7なら `5:2` or `6:1`)
    * **Strong Even (MA3 <= 2.5):** 偶数順張り `2:4` or `1:5` (Loto7なら `2:5` or `1:6`)
    * **Neutral:** 平均回帰 `3:3` (Loto7なら `4:3` or `3:4`)

### 4.2. 合計値のリバウンド予測
* **ロジック:**
    * 前回 `Low基準値以下` → 次回 急騰予測 (高数字必須)
    * 前回 `High基準値以上` → 次回 急落予測 (低数字必須)

---

## 5. 候補選定とフィルタリング (Refined Selection)

### 5.1. 候補プールの作成
* 重力スコア合計が高い順に **上位18個** を抽出。
* 全滅ゾーンを作らないよう、各ゾーン最低1個は確保。

### 5.2. キャリーオーバー相関分析 (CO Bias Logic)
* **目的:** キャリーオーバー(CO)発生時の「市場の歪み」と「物理的偏り」を利用する。
* **定義:** 高額CO時（5億円以上）における数字の出現率偏差を計算。
* **ロジック:** 過去の高額CO回における各数字の出現率を、通常時の出現率と比較。
    * 偏差が正（プラス）に大きい数字（CO時に出やすい数字）に `Score += 偏差 * 5.0` のボーナスを加算。
    * 一般的な「誕生日数字」への偏りを補正する役割も持つ。

### 5.3. KILLスイッチ（絶対除外）
* **連番:** セクション2.4の定義（4連番または5連番）に従い除外。
* **合計値:** 信頼区間外（Loto6: 90-185外 / Loto7: 100-200外）は除外。
* **過集中:** 1ゾーン5個以上は除外。
* **完全偏り:** 奇数/偶数のみ、Lowのみ、Highのみは除外。

---

## 6. アルゴリズム実行フロー

1.  **Initialize:** 公式サイトから最新データ（本数字・CO額）を取得しDB更新。
2.  **Wave Analysis:** 選択されたモード(6/7)に基づき、全数字の4次元スコアを計算。
3.  **CO Adjustment:** CO額に基づき、特定数字のスコアを補正。
4.  **Candidate Selection:** 補正後スコア上位18個を抽出。
5.  **Trend Decision:** 直近トレンドからターゲット奇遇比・合計値を決定。
6.  **Generation & Kill:** 上位18個から全組み合わせ生成 → KILLスイッチ選別。
7.  **Final Ranking:** 残った組み合わせから、スコア合計No.1かつトレンド合致の **「唯一の1口」** を出力。

---

## 7. 可視化と説明責任 (Visualization Protocol)

**【最重要項目】**
全ての計算根拠と正当性は、以下のグラフによって証明されなければならない。
Python (`Matplotlib` / `Seaborn` / `Plotly`) を使用し、毎回必ず以下の画像を生成すること。

### 7.1. 必須出力グラフ
1.  **重力スコア・ヒートマップ (Gravity Heatmap)**
    * **内容:** 全数字(Loto6: 43個 / Loto7: 37個)の最終スコアをヒートマップで可視化。
    * **要件:** 上位18個の候補数字を「赤枠」または「ハイライト」で囲み、選出理由を視覚的に示す。
2.  **選出理由の内訳チャート (Wave Composition)**
    * **内容:** 最終選出された数字について、そのスコア内訳（Depth, Vertical, Horizontal, Cross, CO-Bonus）を示す**積み上げ棒グラフ**。
    * **目的:** 「深さで選んだのか」「相性で選んだのか」「COボーナスで入ったのか」を一目で理解させる。
3.  **キャリーオーバー相関分布 (CO Bias Scatter)**
    * **内容:** 横軸に数字、縦軸に「CO時の出現偏差」をとった棒グラフ。
    * **目的:** 今回のCO状況下で有利な数字がどれかを視覚的に証明する。
4.  **相互作用マトリクス・ヒートマップ (Interaction Matrix 2D)**
    * **内容:** 全数字（Loto6なら43×43、Loto7なら37×37）のグリッドで、数字同士の相性（共起度）をヒートマップ化する。
    * **目的:** 対角線付近の「斜めの色の偏り（連番引力）」や、特定エリアの「過熱感」を視覚的に発見させるため。ユーザーが直感的に「波」を感じ取るための最重要グラフ。

### 7.2. 精度検証レポート (Validation)
* **損益シミュレーション:** 直近10回で本システムを使った場合の「累積収支」グラフを表示。
* **AI信頼度メーター:** 今回の予想に対するAIの自信度（スコアの突出度）を0-100%で表示。

---
**End of Specification**
